# 丹青有AI 移动端环境变量示例
# 复制本文件为 .env(app.config.js 通过 dotenv 读取,Expo CLI 启动时注入到 expo-constants.extra)
# 注意:修改 .env 后需重启 `npm start` 才会生效

# 后端 API Base URL(对应 .trae/documents/api-contract-v1.md §1.1)
# 本地开发默认指向后端服务(后端默认端口 3000,见 server/src/config/env.ts)
API_BASE_URL=http://localhost:3000/api/v1

# 飞书 OAuth 移动端回调 URI(深链接,scheme=danqing)
# P3-1.4 飞书登录集成时使用,需在飞书应用白名单配置该 URI
FEISHU_REDIRECT_URI_MOBILE=danqing://auth/feishu/callback
